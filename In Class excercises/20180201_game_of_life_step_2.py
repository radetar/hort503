from game_of_life_step_1 import cell
test_cell = cell(1,1,1)
class make_grid:
    def __init__(self,rows,cols):
        self.rows = rows
        self.cols = cols
        self.grid = list()

        for r in range(rows):
            r= list()
        for c in r :
            c= cell()
